apiVersion: v1
kind: Service
metadata:
  name: anymserver-service
spec:
  selector:
    app: anymserver
  ports:
  - port: 80           # External port (browser-friendly)
    targetPort: 7000   # Your container's port
    protocol: TCP
  type: LoadBalancer
