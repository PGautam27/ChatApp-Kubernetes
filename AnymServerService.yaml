apiVersion: v1
kind: Service
metadata:
  name: anymserver-service
spec:
  selector:
    app: anymserver
  ports:
  - port: 7000
    targetPort: 7000
    nodePort: 30002
    protocol: TCP
  type: NodePort
